# fileskitchen
Fluent Python library for file pipeline operations — select, filter, and act on file sets across directory trees. Composable predicates, dry-run mode, and a REPL for interactive exploration.
